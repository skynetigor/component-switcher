/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ComponentSwitcherDirective } from './component-switcher.directive';

describe('Directive: ComponentSwitcher', () => {
  it('should create an instance', () => {
    const directive = new ComponentSwitcherDirective();
    expect(directive).toBeTruthy();
  });
});
